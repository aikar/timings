'use strict';

var gulp = require('gulp');
var run = require( './' );

gulp.task( 'test',  function() {
  return run('echo Hello && sleep 1 && echo World && sleep 1 && echo DONE', {verbosity:3})
    .exec()
    .pipe( gulp.dest( '.' ) );
} );
